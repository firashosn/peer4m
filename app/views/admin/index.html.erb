<%= render 'shared/nav' %>

<div class="container">
	<div class="row">
	<div class="col-md-6">

<%= bootstrap_form_for @admin do |f| %>

<%= f.select :current_institution, options_for_select(User::INSTITUTIONS), hide_label: :true, required:true, inline:true %>

<%= f.select :current_program, options_for_select(User::PROGRAMS), hide_label: :true, required:true, inline:true %>

<%= f.select :role, options_for_select(User::ROLES), hide_label: :true, required:true, inline:true %>

		</div>
	</div>
</div>

<% end %>















<%= render 'shared/internal_footer' %>

<%= render 'shared/modal' %>